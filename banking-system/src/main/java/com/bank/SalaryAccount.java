// The SalaryAccount class extends the Account class, representing a salary account
// A classe SalaryAccount estende a classe Account, representando uma conta salário
public class SalaryAccount extends Account {
    
    // Constructor initializing the initial balance of the salary account
    // Construtor que inicializa o saldo inicial da conta salário
    public SalaryAccount(double initialBalance) {
        super(initialBalance); // Calls the constructor of the parent class (Account)
        // Chama o construtor da classe pai (Account)
    }

    // Implement specific functionalities for salary accounts, if necessary
    // Implementar funcionalidades específicas para contas salário, se necessário
}